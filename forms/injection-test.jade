form(title='Injection Test')
  fields
    inject
      |  <h1 style="text-align:center;color:#f00;">Testing<br />HTML injection...</h1>
    notice(text='The above header will not display unless the "inject" element is enabled in config/config.toml.')
  views
  outputs
